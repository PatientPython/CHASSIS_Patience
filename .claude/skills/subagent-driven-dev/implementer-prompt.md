Implement current task with minimal scope.

Rules:
- allowed_tools: all
- edit only planned files unless strictly required
- keep interfaces aligned with `interface_specs`
- produce verification evidence via `keil-build`
